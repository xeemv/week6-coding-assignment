/**
 * create a variable that's going to import the chai expect method called chai expect 
 */
var expect = chai.expect;

// write unit test
describe('Card Wars', function(){
    describe('Class Deck', function(){ // can change when ready, just setting this up
        it ('Should ') // can change when ready, just setting this up 
    })
})